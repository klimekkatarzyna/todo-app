import React, { FC } from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
	padding: 0.5rem 1rem;
	font-size: 0.9rem;
`;

export const GroupedLists: FC = () => {
	return <Wrapper>Przeciągnij tutaj aby dodać listy</Wrapper>;
};
